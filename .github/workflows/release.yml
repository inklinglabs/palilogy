# Inkling Labs standard release workflow for Xcode macOS apps.
# Trigger: Matt pushes a v* tag on main. See dev-standards/STANDARDS.md.
# Replace Palilogy with the app/scheme name (project-init does this).
#
# Required repo secrets (canonical names, no exceptions):
#   APPLE_TEAM_ID, MACOS_CERTIFICATE, MACOS_CERTIFICATE_PWD,
#   ASC_API_KEY, ASC_API_KEY_ID, ASC_API_ISSUER_ID

name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Version from tag
        id: version
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          PLIST=$(/usr/libexec/PlistBuddy -c "Print CFBundleShortVersionString" Palilogy/Info.plist)
          if [ "$PLIST" != "$VERSION" ]; then
            echo "Tag $GITHUB_REF_NAME does not match Info.plist version $PLIST" >&2
            exit 1
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Import signing certificate
        env:
          MACOS_CERTIFICATE: ${{ secrets.MACOS_CERTIFICATE }}
          MACOS_CERTIFICATE_PWD: ${{ secrets.MACOS_CERTIFICATE_PWD }}
        run: |
          KEYCHAIN_PWD=$(uuidgen)
          KEYCHAIN=$RUNNER_TEMP/build.keychain-db
          echo "$MACOS_CERTIFICATE" | base64 --decode > $RUNNER_TEMP/certificate.p12
          security create-keychain -p "$KEYCHAIN_PWD" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "$KEYCHAIN_PWD" "$KEYCHAIN"
          security import $RUNNER_TEMP/certificate.p12 -P "$MACOS_CERTIFICATE_PWD" \
            -A -t cert -f pkcs12 -k "$KEYCHAIN"
          security set-key-partition-list -S apple-tool:,apple:,codesign: \
            -s -k "$KEYCHAIN_PWD" "$KEYCHAIN"
          security list-keychain -d user -s "$KEYCHAIN" login.keychain
          rm $RUNNER_TEMP/certificate.p12

      - name: Build and sign
        run: |
          xcodebuild -project Palilogy.xcodeproj \
            -scheme Palilogy \
            -configuration Release \
            -derivedDataPath build \
            build \
            CODE_SIGN_STYLE=Manual \
            CODE_SIGN_IDENTITY="Developer ID Application" \
            DEVELOPMENT_TEAM="${{ secrets.APPLE_TEAM_ID }}" \
            CODE_SIGN_INJECT_BASE_ENTITLEMENTS=NO \
            OTHER_CODE_SIGN_FLAGS="--timestamp --options=runtime"
          codesign --verify --deep --strict --verbose=2 \
            build/Build/Products/Release/Palilogy.app

      - name: Package DMG
        run: |
          STAGING=$(mktemp -d)
          cp -R build/Build/Products/Release/Palilogy.app "$STAGING/"
          ln -s /Applications "$STAGING/Applications"
          hdiutil create \
            -volname "Palilogy ${{ steps.version.outputs.version }}" \
            -srcfolder "$STAGING" \
            -fs HFS+ \
            -format UDZO \
            "Palilogy-${{ steps.version.outputs.version }}.dmg"

      - name: Notarize and staple
        env:
          ASC_API_KEY: ${{ secrets.ASC_API_KEY }}
          ASC_API_KEY_ID: ${{ secrets.ASC_API_KEY_ID }}
          ASC_API_ISSUER_ID: ${{ secrets.ASC_API_ISSUER_ID }}
        run: |
          echo "$ASC_API_KEY" | base64 --decode > $RUNNER_TEMP/AuthKey.p8
          xcrun notarytool submit "Palilogy-${{ steps.version.outputs.version }}.dmg" \
            --key $RUNNER_TEMP/AuthKey.p8 \
            --key-id "$ASC_API_KEY_ID" \
            --issuer "$ASC_API_ISSUER_ID" \
            --wait
          xcrun stapler staple "Palilogy-${{ steps.version.outputs.version }}.dmg"
          xcrun stapler validate "Palilogy-${{ steps.version.outputs.version }}.dmg"
          rm $RUNNER_TEMP/AuthKey.p8

      - name: Create GitHub release
        run: |
          gh release create "$GITHUB_REF_NAME" \
            "Palilogy-${{ steps.version.outputs.version }}.dmg" \
            --repo "$GITHUB_REPOSITORY" \
            --title "Palilogy ${{ steps.version.outputs.version }}" \
            --generate-notes
        env:
          GH_TOKEN: ${{ github.token }}
